
const Layout = ({children}) => {
    // console.log(children);
    return(
        <div>
            <main>
                {children}
            </main>
        </div>
    )
};

export default Layout;